spring:
  application:
    name: apiMensagem
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/api_mensagem}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  mensageria:
    rabbit:
      exchange: fretes.exchange
      queue: fretes.eventos.queue
      routing-key: fretes.eventos
    websocket:
      topic-fretes: /topic/fretes
